#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	double potencia, raiz, seno, coseno, tangente;
	
	potencia = pow(2, 10);
	raiz = sqrt(13);
	seno = sin(90);
	coseno = cos(180);
	tangente = tan(1.444444);
	
	cout<<"Potencia: "<<potencia<<endl;
	cout<<"Raiz: "<<raiz<<endl;
	cout<<"Seno: "<<seno<<endl;
	cout<<"Coseno: "<<coseno<<endl;
	cout<<"Tangente: "<<tangente<<endl;
	
	return 0;
}
