// Mi primer programa.

// La línea de abajo es una directiva. Se leerá e interpretará antes de que el código se compile.
#include <iostream> // Biblioteca para las operaciones de entrada y salida estándar.

using namespace std; // Esta línea nos ahorra el uso de std:: antes de cada función de la librería estandar.

// Función principal
int main()
{	
	// (ST)andar(D) (C)haracter (OUT)put
	// "<<" - operador de inserción, indica que lo que le siga será insertado a cout
	cout<<"Hola mundo"<<endl;
	
	return false;
}
